//Counting words

#include <iostream>
#include <stdio.h>
using namespace std;
int main(){
	char str[20];
	int count;
	
	cout<<"Enter a string: ";
	gets(str);
	
	for(int i=0; str[i]!='\0'; i++){
		if (isupper(str[i]))
			count++;
	}
	count+=1;
	
	cout<<"\nTotal words: "<<count;
}
